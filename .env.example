# Credentials are stored in chrome.storage.sync via the popup Settings UI.
# This file documents the storage keys used:
#
# apiKey         — Mnemo API key
# workspaceId    — Mnemo workspace ID
# spaceMap       — JSON mapping from chrome groupId to Arc Space label
